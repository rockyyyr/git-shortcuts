# Git Shortcuts

Shortcuts for git commit and git push. Pushing assumes the remote is named `origin`.

## Usage

Commit to current branch

```bash
$ commit <message>
```

Push after commit
```bash
$ push
```

Commit and Push
```bash
$ push <message>
```
